# Frontend API endpoint (set to http://<EC2_PUBLIC_IP>/api for production EC2, or /api when behind Nginx proxy)
VITE_API_URL=/api

# Optional Razorpay Key ID for client-side checkout modal
VITE_RAZORPAY_KEY_ID=
